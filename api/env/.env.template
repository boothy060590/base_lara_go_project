# Database Configuration
# MySQL (default)
DB_CONNECTION=mysql
DB_HOST=db
DB_PORT=3306
DB_USER=api_user
DB_PASSWORD=b4s3L4r4G0212!
DB_NAME=dev_base_lara_go
DB_CHARSET=utf8mb4
DB_COLLATION=utf8mb4_unicode_ci
DB_PREFIX=
DB_STRICT=true
DB_ENGINE=InnoDB

# PostgreSQL (uncomment to use)
# DB_CONNECTION=postgres
# DB_HOST=postgres
# DB_PORT=5432
# DB_USER=postgres
# DB_PASSWORD=postgres_password
# DB_NAME=dev_base_lara_go
# DB_CHARSET=utf8
# DB_PREFIX=
# DB_SCHEMA=public
# DB_SSLMODE=disable

# SQLite (uncomment to use)
# DB_CONNECTION=sqlite
# DB_DATABASE=database.sqlite
# DB_PREFIX=

API_SECRET=yoursecretstring
TOKEN_HOUR_LIFESPAN=1

# Mail Configuration
MAIL_MAILER=smtp
MAIL_HOST=mail.{{APP_DOMAIN}}
MAIL_PORT=1025
MAIL_FROM_ADDRESS=no-reply@{{APP_DOMAIN}}
MAIL_FROM_NAME="Base Laravel Go Project"

# Queue Configuration (ElasticMQ)
# Queue Connection (sqs or sync)
QUEUE_CONNECTION=sqs
SQS_ACCESS_KEY=local
SQS_SECRET_KEY=local
SQS_REGION=us-east-1
SQS_QUEUE=default
SQS_ENDPOINT=http://sqs.{{APP_DOMAIN}}:9324

APP_PORT=8080

# Redis Configuration
REDIS_HOST=redis
REDIS_PASSWORD=null
REDIS_PORT=6379
CACHE_STORE=redis
CACHE_PREFIX=

SENTRY_SECRET_KEY=pTbZ7+ovo1yV3tSnuWM4+CwZ5dHQLPl+VFKXfYeWICI=

# Cache Configuration
# Local (default)
# CACHE_STORE=local
# CACHE_PREFIX=base_lara_go_cache_
# CACHE_TTL=3600

# Redis (uncomment to use)
# CACHE_STORE=redis
# REDIS_HOST=redis
# REDIS_PORT=6379
# REDIS_PASSWORD=null
# REDIS_DB=0

# Multi-Worker Queue Configuration
# Set to true to enable multi-worker mode (separate workers for different queue types)
QUEUE_MULTI_WORKER=false

# Default worker type for single-worker mode
QUEUE_DEFAULT_WORKER=default

# Worker-specific queue assignments
SQS_QUEUE_JOBS=jobs
SQS_QUEUE_MAIL=mail
SQS_QUEUE_EVENTS=events

# Default worker configuration (handles all queues in single-worker mode)
WORKER_MAX_JOBS=1000
WORKER_MEMORY_LIMIT=128
WORKER_TIMEOUT=60
WORKER_SLEEP=3
WORKER_TRIES=3

# Jobs worker configuration (high processing, long-running jobs)
JOBS_WORKER_MAX_JOBS=1000
JOBS_WORKER_MEMORY_LIMIT=128
JOBS_WORKER_TIMEOUT=60
JOBS_WORKER_SLEEP=3
JOBS_WORKER_TRIES=3

# Mail worker configuration (email sending, moderate processing)
MAIL_WORKER_MAX_JOBS=500
MAIL_WORKER_MEMORY_LIMIT=64
MAIL_WORKER_TIMEOUT=30
MAIL_WORKER_SLEEP=3
MAIL_WORKER_TRIES=3

# Events worker configuration (event processing, high throughput)
EVENTS_WORKER_MAX_JOBS=2000
EVENTS_WORKER_MEMORY_LIMIT=256
EVENTS_WORKER_TIMEOUT=120
EVENTS_WORKER_SLEEP=1
EVENTS_WORKER_TRIES=1

# Logging Configuration
LOG_CHANNEL=sentry
LOG_LEVEL=debug
LOG_PATH=storage/logs/laravel.log

# Multi-channel logging
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_DEPRECATIONS_TRACE=false

# Slack logging (uncomment to enable)
# LOG_SLACK_WEBHOOK_URL=https://hooks.slack.com/services/YOUR/SLACK/WEBHOOK
# LOG_SLACK_USERNAME=Laravel Log
# LOG_SLACK_EMOJI=:boom:

# Sentry logging (uncomment to enable)
# SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id
